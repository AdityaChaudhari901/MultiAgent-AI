# utils/llm_client.py
import os
# This is a placeholder. Configure OpenAI/Gemini/other provider here.
# Example with OpenAI (openai package must be installed and API key in env):
# import openai
# openai.api_key = os.getenv("OPENAI_API_KEY")

def parse_intent_with_llm(prompt: str) -> dict:
    """
    Replace this function with a proper LLM call that returns structured JSON.
    For now it returns an empty dict.
    """
    # TODO: implement LLM parse using OpenAI/Gemini via langchain if desired.
    return {}
